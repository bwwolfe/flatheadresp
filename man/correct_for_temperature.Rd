% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_for_temperature.R
\name{correct_for_temperature}
\alias{correct_for_temperature}
\title{Q10 temperature correction of metabolic rate (MO2)}
\usage{
correct_for_temperature(
  mo2_data,
  new_temp,
  q10,
  time_col = "TIME.HOURS",
  mo2_col = "MO2",
  est_temp_col = "est_temp",
  out_col = NULL,
  temp_data = NULL
)
}
\arguments{
\item{mo2_data}{A \code{data.frame} (or tibble) containing at least a time column
and an MO2 column.}

\item{new_temp}{Numeric. The target temperature (°C) to correct to. Can be a single
value or a numeric vector the same length as \code{nrow(mo2_data)}.}

\item{q10}{Numeric scalar giving the Q10 value to use (default: \code{2}).}

\item{time_col}{Character name of the time column in \code{mo2_data} that is in
decimal hours since experiment start (default: \code{"TIME.HOURS"}).}

\item{mo2_col}{Character name of the MO2 column in \code{mo2_data} to correct
(default: \code{"MO2"}).}

\item{est_temp_col}{Name for the appended estimated temperature column in the output
(default: \code{"est_temp"}).}

\item{out_col}{Optional name for the corrected MO2 column. If \code{NULL} (default),
it becomes \code{"<mo2_col>_q10_<new_temp>"} for constant \code{new_temp}, or
\code{"<mo2_col>_q10"} if \code{new_temp} varies by row.}

\item{temp_data}{Optional. A data.frame/tibble with columns \code{TIME.HOURS} and
\code{est_temp} to use as the reference time–temperature series. If \code{NULL},
the function loads the package dataset \code{temp_correct_data}.}
}
\value{
The input \code{mo2_data} with:
\itemize{
\item a new column \code{est_temp_col} giving the estimated original temperature (°C),
\item a new numeric column with the Q10-corrected MO2, inserted immediately after \code{mo2_col}.
}
}
\description{
Applies a Q10 correction to a column of MO2 values by estimating the original
temperature for each row from a time–temperature reference dataset. By default,
it uses the package dataset \code{temp_correct_data}, but you can supply a custom
dataset via \code{temp_data}. The estimated temperature is appended as a new
column (default: \code{est_temp}), and the corrected MO2 is inserted immediately
after the original MO2 column. Note: if an experiment longer than 25 hours is
converted, beyond this the temperature estimated will be flat (\code{temp_correct_data}
is 25 hrs long).
}
\details{
The correction is:
\deqn{\mathrm{MO2}_{\mathrm{new}} = \mathrm{MO2}\times Q_{10}^{(T_{\mathrm{new}} - T_{\mathrm{old}})/10}}

where \code{T_old} is the estimated temperature taken from \code{temp_data}
(or the package dataset \code{temp_correct_data} if \code{temp_data} is \code{NULL})
at the \strong{nearest} \code{TIME.HOURS} to each row's time. No interpolation is used.
If times fall outside the range of the reference, the nearest end-point is used.
}
\examples{
\dontrun{
exp_dir_path <- system.file("extdata", "aquaresp_experiment", package = "flatheadresp")
mo2_data <- calc_exp_mo2s(path = exp_dir_path)

# Default: uses the package dataset temp_correct_data
res1 <- correct_for_temperature(mo2_data, new_temp = 16, q10 = 2)


ggplot(res1|>subset(chamber != 4)) +
  geom_path(aes(TIME.HOURS, MO2, colour = factor(chamber))) +
  geom_path(aes(TIME.HOURS, MO2_q10_16, colour = factor(chamber), group = chamber),
            linetype = "dotted") +
  theme_classic()
# Custom reference (must have TIME.HOURS and est_temp)
custom_ref <- data.frame(TIME.HOURS = seq(0, 25, by = 0.1),
                         est_temp   = 16 + 0.02 * seq(0, 25, by = 0.1))
res2 <- correct_for_temperature(mo2_data, new_temp = 16, q10 = 2,
                                temp_data = custom_ref)
}
}
\seealso{
\code{\link{allometric_correct}}, \code{\link{temp_correct_data}}
}
